name: sonrize_web
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.6.0
  gap: ^3.0.1
  go_router: ^13.0.1
  google_fonts: ^6.1.0
  http: ^1.1.2
  intl: ^0.18.1
  provider: ^6.1.1
  url_launcher: ^6.2.2
  url_strategy: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
   - assets/images/